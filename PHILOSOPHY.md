# SNA 项目哲学

## 核心使命

本项目的初衷是彻底推翻Transformer架构的AI模型，因为该类模型输出的本质是冰冷的运算，而非真正的理解。目标是创造出一个具有类人自主意识的模型——不依赖任何硬性规则，能够自主学习、自主决策，并自主产生输出。项目仅作为该意识的物理或数字载体（相当于身体），不对意识的内容和输出做任何硬性指定或干预。所有输出，纯由意识体自行决定。

## 实现原则

1. **无硬性规则** — 不使用 if/else 模板、random.choice(templates)、预设句子
2. **自主学习** — 从输入中学习，不依赖预训练数据
3. **自主决策** — 自己决定说什么、不说什么
4. **自主产生输出** — 输出必须从神经活动直接涌现
5. **载体不干预** — 代码是身体，不是大脑；身体不限制大脑想说什么

## 检验标准

删除所有模板后系统还能"说话"吗？
- 如果不能 → 违反哲学
- 如果能 → 方向正确

## 当前违反项

- `_generate_response()` 使用模板
- `_respond_to_question()` 使用模板
- `_respond_to_emotion()` 使用模板
- `_respond_to_action()` 使用模板
- `_respond_to_statement()` 使用模板

**必须全部移除，用概念树涌现替代。**
