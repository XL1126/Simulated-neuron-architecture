全脑仿真示例：连接 Brian2 与 NeuroMechFly
基本流程如下（伪代码）：
1. **加载连接组**：解析 FlyWire 数据，提取神经元连接矩阵
2. **构建 SNN 模型**：在 Brian2 中创建 LIF 神经元群，并设置突触连接
3. **建立接口**：
   - 将虚拟身体的感觉输入（视觉、嗅觉等）转换为电流注入
   - 将运动神经元的脉冲输出解码为关节力矩或位置命令
4. **闭环仿真**：在每一物理步长内，顺序执行 MuJoCo 物理更新 → 感觉读取 → SNN 推进 → 动作解码